extends ../../layouts/default.pug
include ../../mixins/table-tree.pug


block main
    
    h1 Danh sách sản phẩm
        .card.mb-3 
            .card-header Danh sách 
                .card-body 
                    .row                         
                        .col-4
                            a(href=`${prefixAdmin}/product-category/create` class="btn btn-primary float-right") Thêm sản phẩm mới
            
    table(
        class="table table-hover table-sm"
        checkbox-multi
    ) 
        thead 
            tr 
                th 
                    input(type="checkbox" name="checkall")
                th STT 
                th Hình ảnh 
                th Tiêu đề
                th Vị trí
                th Trạng thái
                th Hành động
        tbody 
            +table-tree(records)